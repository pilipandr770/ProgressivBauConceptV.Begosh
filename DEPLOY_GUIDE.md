# Деплой на Render.com - Инструкция

## ✅ Проект готов к деплою!

Все необходимые файлы созданы:
- `requirements.txt` - зависимости Python (включая gunicorn)
- `runtime.txt` - версия Python 3.11.0
- `render.yaml` - конфигурация для Render
- `Procfile` - команда запуска для Render

## 📋 Шаги для деплоя на Render.com:

### 1. Создайте аккаунт на Render.com
- Перейдите на https://render.com
- Зарегистрируйтесь (можно через GitHub)

### 2. Подключите GitHub репозиторий
- В Render Dashboard нажмите "New +"
- Выберите "Web Service"
- Подключите свой GitHub аккаунт
- Выберите репозиторий: `pilipandr770/ProgressivBauConceptV.Begosh`

### 3. Настройте Web Service
**Основные настройки:**
- **Name:** progressivbauconcept (или любое другое)
- **Region:** Frankfurt (EU Central) - ближе к клиентам!
- **Branch:** main
- **Root Directory:** (оставьте пустым)
- **Runtime:** Python 3
- **Build Command:** `pip install -r requirements.txt`
- **Start Command:** `gunicorn run:app`

### 4. Добавьте переменные окружения (Environment Variables)
Нажмите "Advanced" и добавьте:
- **Key:** `OPENAI_API_KEY`
- **Value:** ваш API ключ от OpenAI (из .env файла)

### 5. Выберите план
- **Free Plan** - для тестирования (ограничения: засыпает после 15 мин неактивности)
- **Starter Plan ($7/мес)** - для production (всегда активен, без ограничений)

### 6. Нажмите "Create Web Service"
Render автоматически:
- Клонирует репозиторий
- Установит зависимости
- Запустит приложение
- Предоставит URL (например: https://progressivbauconcept.onrender.com)

## 🔧 После деплоя:

### Проверьте работу:
1. Откройте URL вашего сайта
2. Протестируйте все страницы
3. Проверьте чат-ботов (если добавили OPENAI_API_KEY)
4. Убедитесь, что формы работают

### Настройте Custom Domain (опционально):
1. В Render Dashboard → Settings → Custom Domains
2. Добавьте ваш домен (например: progressivbau.de)
3. Обновите DNS записи у вашего регистратора
4. Render автоматически выдаст SSL сертификат

## 🚀 Автоматические обновления:

При каждом `git push` в репозиторий GitHub, Render автоматически:
1. Заметит изменения
2. Пересоберет приложение
3. Задеплоит новую версию

## 📊 Мониторинг:

В Render Dashboard вы можете:
- Смотреть логи приложения
- Отслеживать использование ресурсов
- Получать уведомления о деплое
- Проверять метрики производительности

## ⚠️ Важные замечания:

1. **Free Plan ограничения:**
   - Сервис засыпает после 15 мин без запросов
   - Первый запрос после пробуждения занимает 30-60 сек
   - 750 часов/месяц активности (достаточно для тестирования)

2. **OpenAI API Key:**
   - Обязательно добавьте в Environment Variables
   - Без него чат-боты не будут работать (будет fallback текст)

3. **Статические файлы:**
   - Flask автоматически отдает файлы из `/static`
   - Все изображения, CSS, JS уже настроены

4. **База данных:**
   - Сейчас используется JSON (файлы в `/data`)
   - Для production рекомендуется PostgreSQL (можно добавить в Render)

## 🔗 Полезные ссылки:

- Render Documentation: https://render.com/docs
- Flask Deployment Guide: https://flask.palletsprojects.com/en/2.3.x/deploying/
- Gunicorn Settings: https://docs.gunicorn.org/en/stable/settings.html

## 💡 Советы:

1. После деплоя обновите:
   - Meta теги (замените localhost на реальный домен)
   - Sitemap.xml (обновите URLs)
   - Google Search Console (добавьте новый домен)

2. Настройте Google Analytics для отслеживания посетителей

3. Включите HTTPS (Render делает это автоматически)

4. Регулярно проверяйте логи на наличие ошибок

## 🎉 Готово!

Ваш сайт ProgressivBauConceptV.Begosh теперь доступен онлайн!
